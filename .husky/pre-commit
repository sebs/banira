#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Fast, incremental checks against the already-installed tree.
# Full clean/bootstrap is reserved for CI and `npm run reset`.
npm run lint || exit 1
make test || exit 1
