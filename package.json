{
  "name": "vanillin-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.10.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "reset": "make clean && make bootstrap && make test",
    "clean": "make clean",
    "bootstrap": "make bootstrap",
    "test": "make test",
    "build": "npm run build -ws"
  }
}
