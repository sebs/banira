{
  "name": "vanillin-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tsx": "4.19.2"
  },
  "scripts": {
    "reset": "make clean && make bootstrap && make test",
    "clean": "make clean",
    "bootstrap": "make bootstrap",
    "test": "make test"
  }
}
