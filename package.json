{
  "name": "banira-monorepo",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.19.0"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "tsx": "4.19.2",
    "typedoc": "0.27.6",
    "typescript": "5.7.2"
  },
  "scripts": {
    "prepare": "npx husky",
    "check-versions": "node scripts/check-versions.mjs",
    "preversion": "node scripts/check-versions.mjs",
    "version": "npm version $npm_package_version --no-git-tag-version --allow-same-version -w banira -w banira-cli && git add packages/banira/package.json packages/banira-cli/package.json package-lock.json",
    "reset": "make clean && make bootstrap && make test && make docs",
    "clean": "make clean",
    "bootstrap": "make bootstrap",
    "test": "make test",
    "lint": "make lint"
  }
}
